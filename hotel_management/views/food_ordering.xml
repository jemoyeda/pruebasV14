<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--    Tree View  -->
    <record id="view_hotel_food_ordering_tree" model="ir.ui.view">
        <field name="name">hotel.food.ordering.tree</field>
        <field name="model">hotel.food.order</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!--    Form views-->
    <record id="view_hotel_food_order_form" model="ir.ui.view">
        <field name="name">hotel.food.order.form</field>
        <field name="model">hotel.food.order</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="guest_name"/>
                            <field name="order_time"/>
                            <field name="food_category_ids" string="Category" widget="many2many_tags"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="food_menu" string="Menu">
                            <field name="product_ids" widget="many2many" options="{'not_delete': True}">
                                <kanban quick_create="false" create="true" delete="true">
                                    <field name="id"/>
                                    <field name="name"/>
                                    <templates>
                                        <t t-name="kanban-box">
                                            <div class="oe_kanban_global_click" style="max-width: 200px">
                                                <div class="o_kanban_record_top">
                                                    <img t-att-src="kanban_image('product.product', 'image_1920', record.id.raw_value)"
                                                         class="oe_avatar oe_kanban_avatar_smallbox o_image_40_cover mb0"
                                                         alt="Avatar"/>
                                                    <div class="o_kanban_record_headings ml8">
                                                        <strong class="o_kanban_record_title">
                                                            <field name="name"/>
                                                        </strong>
                                                        <br/>
                                                        <field name="category_id"/>
                                                        <br/>
                                                        <div name="food_product_lst_price" class="mt-1">
                                                            Price:
                                                            <field name="lst_price" widget="monetary"
                                                                   options="{'currency_field': 'currency_id', 'field_digits': True}"/>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </t>
                                    </templates>
                                </kanban>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
